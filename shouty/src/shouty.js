class Person {

    constructor(name) {
        this.name = name
    }

    moveTo = (distance) => {}

    shout = (message) => {}

    messagesHeard = () => ['free bagels at Sean\'s']
}

module.exports = {Person}